coverage:
  status:
    project:
      default:
        target: 98%
        threshold: 0%
    patch:
      default:
        target: 98%
        threshold: 0%

comment:
  layout: " diff, flags, files"
  behavior: default
  require_changes: false
  require_base: true
  require_head: true
  hide_project_coverage: false
